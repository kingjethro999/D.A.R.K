# Embedded at build time into the release APK (see app/build.gradle.kts).
# Copy this file to secrets.properties and fill in your real keys.
# secrets.properties is gitignored - never commit live keys.
GROQ_API_KEY=your_groq_key_here
FIRECRAWL_API_KEY=your_firecrawl_key_here
